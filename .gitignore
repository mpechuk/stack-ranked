.DS_Store
__pycache__/

# Generated PDFs are published as GitHub Release assets, not tracked in the
# repo. The publish-pdfs workflow rebuilds and releases them on every merge to
# main that touches a PDF source; run scripts/publish_pdf_release.sh to publish
# by hand. See README "Printable PDFs".
docs/*.pdf

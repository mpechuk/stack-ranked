{
  "name": "synergy-corp",
  "version": "1.0.0",
  "private": true,
  "description": "Synergy Corp — dependency-free browser game (corporate career-ladder engine builder). Pure-JS rules engine, runnable headlessly in Node.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "sim:balance": "node synergy_corp_montecarlo.js",
    "sim:transfer": "node synergy_corp_manager_switch_test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
